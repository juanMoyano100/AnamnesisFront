import { NoPathologicalPersonalHistory } from './no-pathological-personal-history';

describe('NoPathologicalPersonalHistory', () => {
  it('should create an instance', () => {
    expect(new NoPathologicalPersonalHistory()).toBeTruthy();
  });
});
