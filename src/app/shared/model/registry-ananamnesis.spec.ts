import { RegistryAnanamnesis } from './registry-ananamnesis';

describe('RegistryAnanamnesis', () => {
  it('should create an instance', () => {
    expect(new RegistryAnanamnesis()).toBeTruthy();
  });
});
