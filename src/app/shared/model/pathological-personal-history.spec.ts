import { PathologicalPersonalHistory } from './pathological-personal-history';

describe('PathologicalPersonalHistory', () => {
  it('should create an instance', () => {
    expect(new PathologicalPersonalHistory()).toBeTruthy();
  });
});
